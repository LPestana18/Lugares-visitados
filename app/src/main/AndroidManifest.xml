<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.lucaspestana.lugares_visitados">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gps.GpsActivity" android:label="@string/onde_estou_localizado">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".ui.AddLugarActivity"/>
        </activity>

        <activity android:name=".ui.NovoUsuarioActivity" android:label="@string/cadastrar">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".ui.MainActivity"/>
        </activity>

        <activity android:name=".ui.ListaLugaresActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.AddLugarActivity" android:label="@string/novo_lugar">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".ui.ListaLugaresActivity"/>
        </activity>

        <activity android:name=".ui.MainActivity"/>
    </application>

</manifest>